Pod::Spec.new do |s|
  s.name         = 'JKLogDispatcher'
  s.version      = '0.1.0'
  s.author       = 'Jackie'
  s.requires_arc = true
  s.source_files = 'JKLogDispatcher/**/*.{h,m}'
  s.resources = '**/*.{xib}'
end
